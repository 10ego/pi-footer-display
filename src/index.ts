import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/** Extension behavior will be added after the architecture milestone. */
export default function footerDisplayExtension(_pi: ExtensionAPI): void {}
